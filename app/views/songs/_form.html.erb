<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %><br>
  <%= f.label :artist_name unless @song.artist %>
  <%= f.text_field :artist_name unless @song.artist %>
  <%= artist_id_field(@song) %>
  <br />

  <%= f.submit %>
<% end %>
